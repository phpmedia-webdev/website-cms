import { SubscriptionsListClient } from "./SubscriptionsListClient";

/** Step 34: Subscriptions list — customers and active subscriptions with product, status, current period end; link to Stripe. */
export default function EcommerceSubscriptionsPage() {
  return <SubscriptionsListClient />;
}
