# Automations

Routines that run in response to events (e.g. new member signup). Used to keep CRM and other systems in sync.

- **on-member-signup** – When a new member signs up, ensure they exist in the CRM with status "New" so they can be added to memberships and other flows.
