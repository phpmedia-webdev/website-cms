/**
 * Shared layout tokens for admin multi-suggest pickers (Directory, resources, etc.).
 * Change here to update all surfaces that use the picker wrappers.
 */

/** Input + dropdown container width cap */
export const ADMIN_PICKER_FIELD_CLASS = "max-w-md";

/** Dropdown panel max height (scroll inside panel) */
export const ADMIN_PICKER_DROPDOWN_CLASS = "max-h-48";
